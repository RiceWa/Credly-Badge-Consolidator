# Credly Badge Consolidator


A Streamlit app that merges a Credly Excel export into a master Excel sheet, removes duplicates, and lets you download the updated workbook.

## How to run

```bash
streamlit run app.py
